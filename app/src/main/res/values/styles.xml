<resources>

    <style name="AppTheme" parent="AppTheme.Base">
        <!-- Customize your theme here. -->

    </style>

    <!-- Splash Screen theme. -->
    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/bg_splash</item>
    </style>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="titleTextColor">@color/colorPrimaryText</item>
        <item name="android:textColorHint">@color/colorSecondaryText</item>
        <item name="android:windowBackground">@color/colorWindowBackground</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.Overlay" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
    </style>

    <style name="AppTheme.TabTextStyleBold" parent="TextAppearance.Design.Tab">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.InputBorder" parent="AppTheme">
        <item name="android:padding">@dimen/layout_margin_half</item>
        <item name="background">@drawable/border_layout</item>
    </style>

    <style name="AppTheme.RadioButtonFirst" parent="AppTheme">
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/radio_background</item>
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/radio_color</item>
        <item name="textAllCaps">true</item>
    </style>

    <style name="AppTheme.RadioButton" parent="AppTheme">
        <item name="android:layout_marginLeft">-1dp</item>
        <item name="android:textColor">@color/radio_color</item>
        <item name="android:button">@null</item>
        <item name="android:background">@drawable/radio_background</item>
        <item name="android:gravity">center</item>
        <item name="textAllCaps">true</item>
    </style>
</resources>
